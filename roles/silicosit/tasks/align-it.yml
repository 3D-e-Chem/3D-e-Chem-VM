---
- unarchive:
    src: http://silicos-it.be.s3-website-eu-west-1.amazonaws.com/_downloads/align-it-1.0.4.tar.gz
    dest: /opt/align-it
    remote_src: True
- name: Build dir
  file:
    path: /opt/align-it/build
    state: directory
- name: cmake
  command: cmake ..
  args:
    chdir: /opt/align-it/build
    creates: Makefile
- name: make install
  make:
    chdir: /opt/align-it/build
    target: install
- name: Clean up
  file:
    path: /opt/align-it
    state: absent